import AlertBanner from './index.svelte'

export default {
    title: 'Alert Banner',
    component: AlertBanner,
}

export const Default = {
   parameters: {
    viewport: {
      defaultViewport: 'bsLg',
    },
  }, 
}